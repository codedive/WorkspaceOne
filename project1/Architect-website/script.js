
document.querySelector('.hamburger-menu').addEventListener('click',()=>{
    document.querySelector(".container").classList.toggle("change") /*classlist return all the classes element has */
  /* so again we have to add class to container when we click icon  and remove it on next click*/
/* so we need to use toogle method and attach it to method toggle */
})


